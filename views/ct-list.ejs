<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <title>podScholar</title>
    <!-- Favicon-->
    <link rel="icon" type="image/x-icon" href="/assets/PodScholars-removebg-preview.png" />

    <!-- Bootstrap icons-->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />

    <!-- Core theme CSS (includes Bootstrap)-->
    <link href="/css/style.css" rel="stylesheet" />
    <link href="/css/bootstrap.css" rel="stylesheet" />
    <link href="/css/header.css" rel="stylesheet" />
    <link href="/css/cards.css" rel="stylesheet" />

    <!--Bootstrap JS/Popper Bundle-->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <!--jquery hosted locally-->
    <script src="/js/jquery-3.6.0.js"></script>

    <script src="/js/lister.js"></script>
</head>

<body>
    <!-- Navigation-->
    <%- include('./components/nav.html') %>
        <!-- Section-->
        <main class="py-4">
            <div class="px-4 px-md-5 mt-3">
                <h3 id="headerLabel" class="aleo700">
                    <%= type %>
                </h3>
                <!--Use ejs to load a different template for tags and categories, but give both templates the same id-->
                <% if (type == 'Keywords') { %>
                    <div class="d-flex">
                        <% for (var i = 0; i < data.length; i++) { %>
                            <a href="/keywords/<%= data[i]._id %>" class="aleo400 link-secondary text-decoration-none mx-2">
                                <%= data[i]._id %>  (<span class="counter" id="<%= data[i]._id %>-count"></span>)
                            </a>
                        <% } %>
                    <% } %>
                    <% if (type == 'Categories') {%>
                        <div class="d-md-flex flex-md-row flex-md-wrap justify-content-md-between">
                        <% for (var i=0; i < data.length; i++){ %>
                            <div class="container card-container">
                                <div class="card my-1">
                                    <div class="card-body">
                                        <a href="/categories/<%= data[i]._id %>" class="card-title link-dark text-decoration-none stretched-link">
                                            <h5 class="aleo400 mb-1"><%= data[i]._id %> (<span class="counter" id="<%= data[i]._id %>-count"></span>)</h5>
                                        </a>
                                        <p class="mx-2">
                                            <%= data[i].description %>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        <% } %>
                    <% } %>
                    <!-- EJS can load the cards here or they can be loaded via ajax in lister.js -->
                </div>
            </div>
            <script> countLoader("<%= type %>") </script>
        </main>
        <!-- Footer-->
        <%- include('./components/footer.html') %>
</body>

</html>